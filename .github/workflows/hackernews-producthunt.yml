name: HackerNews & ProductHunt Radar
on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  intelligence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install dependencies
        run: npm install
      - name: Run Intelligence Gathering
        run: npx tsx scripts/scan-hackernews.ts
      - name: Commit results
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "chore: daily hn/ph intelligence update [skip ci]"
          file_pattern: 'data/reports/hackernews-*.md'
